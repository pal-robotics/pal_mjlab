### Kangaroo locomotion external project for Mjlab

Place the repository in the same directory as mjlab and run:

```bash
uv run scripts/list_envs.py
```

Once done, you can run your tasks using:

```bash
uv run scripts/rl/train.py NAME-OF-TASK-ID
```
